import ProductTypeTable from './components';

/*
import { connect } from 'react-redux';
import { Actions } from '../../../store/redux/product-type';

const mapStateToProps = ({ reportsReducer: { ReportCategories, ReportCategoriesStatus } }) => ({
  ReportCategories,
  ReportCategoriesStatus,
});

const mapDispatchToProps = dispatch => ({
  fetchReportsList: () => dispatch(Actions.fetchReportsList()),
});

export default connect(mapStateToProps, mapDispatchToProps)(ProductTypeTable);
*/

export default ProductTypeTable;